# E-commerce MASTER Preview

Public, neutral preview for the private reusable e-commerce MASTER.

This repository contains only the presentation preview. The reusable application source, client data, credentials, backend configuration, payment secrets and production assets are not stored here.
